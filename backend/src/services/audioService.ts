export const processAudioStream = async (audioBuffer: Buffer): Promise<string> => {
    // Example: Process the audio buffer (e.g., save it, analyze it, etc.)
    console.log('Processing audio buffer of size:', audioBuffer.length);
    return 'Audio processed successfully';
  };
  